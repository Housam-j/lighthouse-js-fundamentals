function eat(food){
  return food + ' tasted really good.' 
}
console.log(eat('bananasfunction smartGarbage(trash, bins){
const keys = Object.keys(bins)

for ( let keys = 0; keys < keys.length; keys++)
{
if (keys == recycling)
{
  
bins += 1;

}
}
return (bins);

}'))